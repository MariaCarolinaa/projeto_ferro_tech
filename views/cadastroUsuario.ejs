<%- include('partials/head') %>
<%- include('partials/navbar') %>

    <div class="container" style="margin-top: 5%;">

        <div class="row">
            <div class="col-sm">
                <p class="text-center display-4 mb-5">Cadastro de Usuário</p>
            </div>
        </div>

        <div class="row">
            <div class="col-sm">
                <form method="POST" action="cadastro/usuario">
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            <label for="login">Login</label>
                            <input type="input" class="form-control" id="login"
                                placeholder="Ex: Antonio" name="login">
                        </div>
                        <div class="form-group col-md-6">
                            <label for="senha">Senha</label>
                            <input type="password" class="form-control" id="senha"
                                 name="senha" maxlength="6">
                        </div>
                    <div class="form-row row d-flex justify-content-center col-md-12">
                        <div class="form-group col-auto-md-3 botoesCadastro">
                            <a class="btn btn-outline-primary-login btn-outline-primary" href="#"
                                role="button">Voltar</a>
                            <button type="submit" class="btn btn-outline-dark ml-4">Cadastrar</button>
                        </div>
                    </div>

                </form>
                <%if(mensagem != null){%>
                <h2><%=mensagem%></h2>
                <%}else{}%>

            </div>
        </div>
    </div>
    </div>
<%- include('partials/footer') %>